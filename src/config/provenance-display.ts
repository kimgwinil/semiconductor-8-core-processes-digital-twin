/**
 * 🔴 **출처·등급 화면 표시 스위치.** 표시만 끈다 — 데이터는 하나도 지우지 않는다.
 *
 * // CEO 지시 2026-08-23 · 2026-08-24 로 비활성. 화면 표시만 비활성. 데이터·출처 사슬은 보존.
 * // 🔴 D-011·D-013: 「경향모델」 등급은 현업 검증(L2)으로만 승급한다.
 * //    대외 배포·현업 검증(L2) 시점에는 되살려야 한다 (A6·D-011·D-013).
 *
 * ── 무엇이 꺼지는가 ────────────────────────────────────────────────
 *  · 등급 배지(「경향모델」·「문헌식」·「검증식」·「운영규약」)
 *  · 출처 배지의 S번호 · 「출처 없음 — 교육용 합성」·「출처 확인 중」·「출처 비공개」
 *  · 합성 고지(「실제 장비 상수가 아닙니다…」)·L2 고지·`assumptions` 목록
 *  · 합격창 근거 배지(「근거 · 교육용 설정값」·「근거 미상」) — `passBasisNode()` 한 곳에서
 *  · 상단 배너(「현업 검증 전 …」) 와 소개 화면의 같은 취지 문단
 *  · 본문 산문에 인라인으로 박힌 S번호 인용(`@/lib/redact-provenance`)
 *
 * ── 무엇이 보존되는가 (⛔ 지우지 마라) ──────────────────────────────
 *  · `sourceId`·`withSource`·`grade`·`kind`·`basis`·`notice`·`assumptions` **데이터 전부**
 *  · `src/content/model-grades.json` 등급 원장 · 서지 원장 · `PASS_BASIS_LEDGER`
 *  · `SourceBadge`·`PassBasisBadge`·`passBasisOf()` **구현 전부** (호출부도 그대로 둔다)
 *
 * 🔴 **되살리는 법: 이 상수 하나를 `true` 로 되돌린다. 그 한 줄뿐이다.**
 *
 * 🔵 2026-08-25 최종 화면 정책 — 사용자 지시로 출처·등급·합성·L2 표시를 숨긴다.
 *    `check-a6b` 는 이 스위치가 true이면 표시 전건, false이면 미표시 전건을 DOM에서 검증한다.
 */
/* 🔵 2026-09-01 — `true` 로 복구. 오케스트레이터 지시(CEO 판정 「8/25 배포 승인됨」 확인 후).
 * 근거는 이 파일이 스스로 적어 둔 조건이다: 「**대외 배포·현업 검증(L2) 시점에는 되살려야 한다
 * (A6·D-011·D-013)**」. 대외 배포는 2026-08-25 09:59 에 실제로 일어났고(공개 저장소 · Pages
 * 자동배포 성공 · HTTP 200) CEO 승인도 확인됐으므로 **그 조건이 이미 충족됐다.**
 * 화면 출력 167개 중 합성이 83개(49.7 %)인데 고지가 꺼져 있으면 학습자가 어느 수가 문헌식이고
 * 어느 것이 교육용 합성인지 구분할 수 없다 — A6-b 3요건 중 ②배지·③고지가 그것이다.
 * 🔴 로직은 한 줄도 바꾸지 않았다. 이 상수 하나뿐이다. */
export const SHOW_PROVENANCE = true;
