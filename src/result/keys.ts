/**
 * 🔴 저장소 키 **정본** — 개인 식별이 붙은 데이터가 남는 자리의 이름은 여기서만 선언한다.
 *
 * 왜 파일을 따로 두는가:
 *  2026-08-21 이전에는 같은 키 문자열이 `sink.ts`·`erasure.ts`·저장 위치 원장 세 곳에
 *  **각각** 적혀 있었다. 값이 우연히 같아서 어떤 테스트도 이것을 잡지 못했다.
 *  🔴 저장 쪽만 키를 올리면 파기기는 옛 키에서 0건을 찾고 `remaining: []` 를 돌려주며,
 *     호출부는 그것을 「파기 완료」로 읽는다 — **조용한 파기 실패**다.
 *     D-038 · LEG `11_파기절차.md` 의 5일 내 파기 의무가 걸린 경로이므로 구조로 막는다.
 *
 * 🔴 **키 값 자체를 바꾸지 마라.** 이미 학습자 브라우저에 저장된 데이터가
 *    파기 순회의 시야에서 사라진다. 부득이 올릴 때는 §RETIRED_RESULT_KEYS 를 읽어라.
 */

/** 제출된 결과 본저장. */
export const RESULT_STORE_KEY = 'cjh.results.v1';

/** 전송 실패 재시도 큐. */
export const RESULT_QUEUE_KEY = 'cjh.results.queue.v1';

/**
 * 🔴 **은퇴한 키** — 더 이상 쓰지 않지만 **데이터가 남아 있을 수 있는** 키.
 *
 * 위 키를 올릴 때는 **옛 값을 반드시 여기에 옮긴다.** 저장 위치 원장이 이 목록을 그대로
 * 실어 파기 순회가 계속 방문하므로, 키를 올려도 옛 데이터가 파기 대상에서 빠지지 않는다.
 * 목록에서 빼도 되는 시점은 「그 키에 데이터가 없음이 확인된 뒤」뿐이다.
 */
export const RETIRED_RESULT_KEYS: readonly string[] = [];

/**
 * 교사용 CSV 내려받기 파일명.
 * 🔴 이 경로는 **내려받는 순간 우리 통제 밖**이라 코드로 지울 수 없다(LEG `11_파기절차.md` §3-B).
 * 원장에는 반드시 오르되 파기 순회는 이것을 「지웠다」로 세지 않는다.
 */
export const RESULT_CSV_FILENAME = 'cjh-results.csv';

/**
 * 🔴 `schema.ts` 의 `RESULT_SCHEMA_VERSION` 과 위 키의 `v1` 은 **무관하다.**
 * 우연히 같은 수일 뿐이며 연동해서는 안 된다. 판단 근거는 `schema.ts` 의 같은 주석에 적었다.
 */
