/**
 * 씬 **공용 화면 배치 상수** — 여러 씬이 같은 뜻으로 쓰는 값의 정본.
 *
 * 왜 이 파일이 생겼나 (2026-08-21):
 *   `AXIS_X = 0.5` 가 `crystalGrowth.model.ts` 와 `aerialImage.model.ts` 에서 **각자 선언**돼
 *   `check-constants` **R1(이름 중복)** 이 발화했다. 값이 같아 화면 문제는 없었지만
 *   **정본이 둘이면 하나는 낡는다** — 이 프로젝트가 이미 여러 번 당한 사고다
 *   (`models/README.md` 의 「폴백이 계산식을 손으로 다시 적어 6종 전부 갈렸다 · 최대 23.06 배」).
 *
 * 🔴 두 하위 담당이 **서로 상대 파일이 편집 금지**라 아무도 못 고치는 교착이었다.
 *    경계 교착은 팀장이 푼다 — 그래서 제3의 정본 자리를 만들었다.
 *
 * 🔴 이 폴더의 규칙(`models/README.md`)을 그대로 따른다:
 *    · GLSL 문자열을 담지 않는다 · `scenes/*.ts` 를 import 하지 않는다
 *    · 좌표계는 셰이더와 같은 **UV(세로 0~1, 위가 +)**
 *
 * 🔴 **여기에는 「여러 씬이 같은 뜻으로 쓰는 값」만 둔다.** 한 씬에만 있는 배치값은
 *    그 씬의 모델 파일에 그대로 둬라. 전부 여기로 모으면 씬별 배치의 응집이 깨진다.
 */

/**
 * 캔버스 가로 중심 — 씬의 **대칭축**.
 *
 * · `crystalGrowth`: 잉곳 **인상축(회전축)**
 * · `aerialImage`: **광축**(렌즈·동공·라인 패턴의 중심)
 *
 * 두 씬 모두 좌우 대칭 구도라 축이 화면 한가운데 선다. **0.5 는 「캔버스의 절반」이라는 뜻**이며
 * 어느 문헌값도 아니다(배치값이다 — `check-sources` 의 허용 리터럴 0.5 에 해당한다).
 */
export const AXIS_X = 0.5;
