/**
 * 🔴 **R-5 래칫 픽스처 — 행번호 인용이 「악화」하면 즉시 실패한다.**
 * 아래에 금지 패턴을 **기준선보다 많이** 심어 둔다. `--r5-baseline 2` 로 돌리면 FAIL 이어야 한다.
 * (통과 방향은 `--r5-baseline 99` 로 확인한다 — 같은 파일로 양방향을 본다.)
 */
export const LINEREF_FIXTURE = 1;

// 명세 100
// 명세 200행
// 03_실습3단계명세.md:300
// 03:400

/* 🔴 R-5 **확장** 형태(E1·E2) — 원 패턴 3종이 못 잡던 것. 별도 기준선으로 래칫한다. */
// PLN `03_실습3단계명세.md` 476행
// PLN 427
