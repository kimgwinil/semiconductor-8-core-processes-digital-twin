/**
 * 🔴 **중첩 인용 픽스처.** 첫 `」` 에서 끊으면 인용문이 잘려 엉뚱한 판정이 난다.
 * 아래 인용문 안에는 `「선형 → 포물선 전이(x ≈ A)」` 캡션이 **또** 들어 있다.
 * 깊이 짝짓기가 동작하면 통째로 뽑혀 「명세에 없다」로 정확히 실패한다
 * (본문 L725 는 「전이 두께 x ≈ A 아래에서는」으로 **정정됐다**).
 */
export const NESTED_FIXTURE = 1;

/* 명세 595 원문: 「두께–시간 그래프의 곡선이 처음 20 nm 구간에서는 직선(기울기 B/A)이다가
 *   그 뒤 √t 곡선으로 꺾이며, 꺾이는 지점에 **「선형 → 포물선 전이(x ≈ A)」** 캡션이 붙는다.」 */
export const THICKNESS_CHART = 'fixture';
