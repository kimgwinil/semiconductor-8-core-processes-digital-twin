/**
 * 🔴 **R-4 v2 정밀화 픽스처 (③) — 「진짜 폐기는 여전히 배제되는가」 · 절 단위(S2).**
 *
 * `03:3050` 은 `> ### 아직 보류인 것 (별건)` 이라는 **표제**이고 L3051–3052 가 그 지배를 받는다.
 * 표제가 가진 「보류」는 **자기 아래를 지배**하므로 S2 로 절 전체가 배제돼야 한다.
 * 아래 인용문은 **L3052 에만** 있다(전수 grep 1건) — 배제되지 않으면 그대로 통과해 버린다.
 *
 * 🔴 이 픽스처가 PASS 로 뒤집히면 표제 지배(S2)가 깨진 것이다.
 */
export const DEPSECTION_FIXTURE = 1;

/* 명세 §P8: 「Tj 판정 전체가 이 값에 걸려 있으므로」 */
export const TJ_PROVISIONAL = true;
