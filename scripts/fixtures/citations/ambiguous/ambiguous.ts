/**
 * 🔴 **AC-R2 픽스처 — 「2건 이상 = 경고」가 첫날부터 실제로 발동한다.**
 * 「판정은 이 차트에서 한다」는 `03` 의 **살아 있는 본문 2곳**(L134·L557)에 있다.
 * 이것을 실패로 만들면 게이트를 첫날부터 못 쓴다 → 반드시 **통과(경고)** 여야 한다.
 */
export const AMBIGUOUS_FIXTURE = 1;

/** 명세 427: 「판정은 이 차트에서 한다」 */
export const JUDGED = true;
