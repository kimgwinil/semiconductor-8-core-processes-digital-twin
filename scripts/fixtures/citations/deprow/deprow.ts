/**
 * 🔴🔴 **R-4 v2 정밀화 픽스처 (②) — 「진짜 폐기는 여전히 배제되는가」 · 행 단위.**
 *
 * 배제를 좁히면서 **R-4 본래 기능을 깨지 않았는지**를 가장 좁은 단위에서 본다.
 * 아래 문구는 `03:2664` **표 한 칸**에만 있고 그 칸은 「🔴 **폐기·대조용**」으로 표시돼 있다.
 * 블록(L2633–2674)은 이제 **살아 있는 것으로 취급**되지만 **이 한 행은 S1 로 배제**돼야 한다.
 *
 * 🔴 이 픽스처가 PASS 로 뒤집히면 좁히다가 R-4 를 잃은 것이다.
 */
export const DEPROW_FIXTURE = 1;

/* 명세 §P7 표: 「구 모범조합 A = 100 mm²」 */
export const OLD_BEST_AREA_MM2 = 100;
