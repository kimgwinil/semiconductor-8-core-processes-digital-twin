/**
 * 🔴🔴 **R-4 v2 정밀화 픽스처 (①) — 「그 블록이 이제 실제로 검사되는가」.**
 *
 * 아래 두 문구는 `03_실습3단계명세.md` 의 **P7 공통 상수 블록(L2633–2674)** 안에만 있다.
 * 그 블록은 **살아 있는 정본**인데, v1 은 **L2664 표 한 칸**의 「폐기·대조용」 낱말 하나 때문에
 * **42행을 통째로 배제**했다. 그래서 이 블록을 참뜻의 자리로 인용하면 **영영 통과할 수 없었다.**
 *   실물 피해: `eds.ts:171` 「공통 상수(교육용 설정값)」의 참뜻의 자리는 L2635 인데 배제돼 있었고,
 *             `fixed.ts:15`·`:16` 「웨이퍼 지름 D = 300 mm」는 L2636 이 배제돼
 *             **「배제영역 1곳 — 거짓 통과였다」는 거짓 경고**를 매번 달았다.
 *
 * 🔴 이 픽스처가 FAIL 로 뒤집히면 배제가 다시 블록 단위로 넓어진 것이다.
 */
export const REVIVED_FIXTURE = 1;

/* PLN `03_실습3단계명세.md` §P7 공통 상수: 「테스터 시간당 운영 단가 = 300,000 원/h」 */
export const TESTER_RATE_KRW_PER_H = 300000;

/* 명세 §P7: 「웨이퍼당 로딩·정렬·언로딩 오버헤드 = 60 s (고정)」 */
export const WAFER_OVERHEAD_S = 60;
