/**
 * 🔴 **생략부호 분할 픽스처.**
 * 저자가 `…` 로 잘라낸 인용은 **조각마다 따로** 대조해야 한다.
 * 아래는 앞 조각이 살아 있고 **뒤 조각이 죽은** 경우 — 전체가 FAIL 이어야 한다.
 * (조각 분할을 안 하면 「이어 붙인 문자열이 없다」로 우연히 FAIL 이 나므로,
 *  앞 조각이 살아 있다는 사실이 출력에 보이는지까지 확인한다.)
 */
export const ELLIPSIS_FIXTURE = 1;

/* 명세 476: 「도펀트를 인(P) → 붕소(B)로 바꾸면 … 이 문구는 명세 어디에도 없는 지어낸 꼬리다.」 */
export const DOPANT_NOTE = 'fixture';
