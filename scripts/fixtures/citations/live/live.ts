/**
 * 🔴 **AC-R4b 반대 방향 픽스처 — 「`>` 면 무조건 폐기」 로 단정하면 이 파일이 거짓 실패한다.**
 *
 * 「세로축 200 ± 3 mm 만 확대」는 `03_실습3단계명세.md` **전체에서 딱 한 곳**(L556)에 있고,
 * 그 한 곳이 **`>` 인용 블록 안**이다(L546–559, 오케스트레이터 판정으로 살아 있는 정본).
 * 배제 규칙을 `>` 만으로 걸면 이 살아 있는 인용이 「명세에 없다」로 잡힌다.
 * 🔴 이 픽스처가 FAIL 로 뒤집히면 배제 규칙이 너무 넓어진 것이다.
 */
export const LIVE_FIXTURE = 1;

/** 명세 426 「세로축 200 ± 3 mm 만 확대」 */
export const DIAMETER_ZOOM_HALF_MM = 3;

/* PLN `03_실습3단계명세.md` 476행 「규격 하한선(수평 빨간 파선, 상단값의 70 %)과의 교차점이
 * **g = 0.48 에서 g = 0.91 로** 이동한다.」 — 여러 줄에 걸친 인용도 이어 붙여 대조해야 한다. */
export const SPEC_FLOOR_RATIO = 0.7;
