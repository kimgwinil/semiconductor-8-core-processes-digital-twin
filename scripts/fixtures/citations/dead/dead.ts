/**
 * 🔴🔴 **AC-R4 픽스처 — 이 게이트가 존재하는 이유 그 자체다.**
 *
 * 아래 인용문 「처음 20 nm 구간에서는 직선」은 **실제 `03_실습3단계명세.md` 안에 물리적으로 존재한다.**
 * 그러나 존재하는 곳은 **삭제 선언 배너 안뿐**이다:
 *
 *   03_실습3단계명세.md:727
 *     `> 🔵 **PLN 정정 2026-08-21 (DP-1 판정) — 「처음 20 nm 구간에서는 직선」을 삭제했다.**`
 *
 * → 「명세 전문에서 완전 일치 검색」만 하는 구현은 이 파일을 **통과**시킨다(거짓 통과).
 *   R-4 가 폐기 영역을 먼저 뺀 구현만 이 파일을 **실패**로 잡는다.
 *   🔴 이 픽스처가 PASS 로 뒤집히면 게이트가 R-4 를 잃은 것이다. 그때는 게이트를 고쳐라.
 *
 * 🔴 이 파일은 **진짜 `03`** 을 상대로 돌린다. 가짜 명세로 대신하면 증명이 되지 않는다.
 */
export const DEAD_FIXTURE = 1;

/* 명세 595 원문: 「처음 20 nm 구간에서는 직선」 */
export const OXIDE_TRANSITION_NOTE = 'fixture';
